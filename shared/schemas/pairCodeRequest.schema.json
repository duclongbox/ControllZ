{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://remotehost.dev/schemas/pairCodeRequest.schema.json",
  "title": "PairCodeRequest",
  "description": "Client -> server. An authenticated desktop asks for a code to display. Requesting a new code invalidates the desktop's previous one, and so does the desktop disconnecting.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type"],
  "properties": {
    "type": {
      "const": "pairCodeRequest"
    }
  }
}
