{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://remotehost.dev/schemas/authenticate.schema.json",
  "title": "Authenticate",
  "description": "Client -> server. Proves identity on every connection after the first with the credential stored at registration. This is what makes reconnects codeless -- pairing codes are first-pairing-only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "deviceId", "credential"],
  "properties": {
    "type": {
      "const": "authenticate"
    },
    "deviceId": {
      "type": "string",
      "format": "uuid"
    },
    "credential": {
      "type": "string"
    }
  }
}
