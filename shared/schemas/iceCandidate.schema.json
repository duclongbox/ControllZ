{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://remotehost.dev/schemas/iceCandidate.schema.json",
  "title": "IceCandidate",
  "description": "Relayed verbatim between peers, trickled as each candidate is gathered. Host, STUN and TURN candidates all travel this one message -- there is no separate LAN path, because ICE already prefers host candidates when both peers are on the same network (CLAUDE.md invariant).",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "sessionId", "candidate"],
  "properties": {
    "type": {
      "const": "iceCandidate"
    },
    "sessionId": {
      "type": "string",
      "format": "uuid"
    },
    "candidate": {
      "type": "string",
      "description": "Opaque SDP candidate line."
    },
    "sdpMid": {
      "type": ["string", "null"]
    },
    "sdpMLineIndex": {
      "type": ["integer", "null"],
      "minimum": 0
    }
  }
}
