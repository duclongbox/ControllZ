{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://remotehost.dev/schemas/pairCodeSubmit.schema.json",
  "title": "PairCodeSubmit",
  "description": "Client -> server. An authenticated phone redeems a code it was shown on the desktop. Rejections are `error` with `invalidPairCode` (unknown, expired or already used -- indistinguishable), `wrongDeviceType`, `selfPairing`, or `rateLimited`.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "code"],
  "properties": {
    "type": {
      "const": "pairCodeSubmit"
    },
    "code": {
      "type": "string",
      "pattern": "^[0-9]{6}$"
    }
  }
}
